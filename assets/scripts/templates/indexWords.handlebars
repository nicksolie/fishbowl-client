{{#each words as |word|}}
  <section class="col-md-6 col-sm-12" data-id="{{word.id}}">
    <h3>
      {{limit word.name 18}}
    </h3>
    <div class="row">
      {{> deleteWord}}
    </div>
    <div class="row">
      {{> updateWord}}
    </div>
  </section>
  <br>
{{/each}}
